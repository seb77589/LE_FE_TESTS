/**
 * Activity feed UI was removed as part of the admin UI simplification.
 */

describe.skip('ActivityList (removed)', () => {
  it('is no longer part of the admin UI', () => {
    expect(true).toBe(true);
  });
});

export {};
